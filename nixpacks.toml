[providers]
python = "3.11"

[phases.setup]
aptPkgs = ["build-essential", "python3-dev", "libffi-dev", "libssl-dev"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python tapsi_bot.py"

